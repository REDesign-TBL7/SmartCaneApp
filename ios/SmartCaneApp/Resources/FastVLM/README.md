FastVLM weights are no longer bundled into the app target.

Download them to a staging folder, then import that folder from the in-app FastVLM setup screen.

Example:

```bash
ios/scripts/get_fastvlm_model.sh 0.5b ios/FastVLMAssets/model
```
